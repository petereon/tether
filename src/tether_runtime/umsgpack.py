# Placeholder for the vendored MicroPython msgpack port.
#
# TODO: vendor a real `umsgpack.py` implementation here (pure-Python,
# MicroPython-compatible msgpack encode/decode). Must stay wire-compatible
# with the `msgpack` package used on the PC side (marshalling/__init__.py).
# See DESIGN.md § Dependencies.
